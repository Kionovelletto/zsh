Para realizar a instação do ZSH em sua distribuição linux, execute o chamando abaixo em seu terminal linux:

```bash
sh -c "$(curl -fsSL https://github.com/Kionovelletto/zsh/blob/main/install_zsh_bira.sh)" 
```
